# v0.5 Acceptance Report

Date: 2026-07-19

## Scope

Validate v0.5 provider preset, prompt-file, discovery, and quoting-policy
behavior:

- migrate existing DB to schema version `3`
- apply schema versions `1`, `2`, and `3` on a fresh DB
- install built-in adapter presets
- discover a local executable-backed adapter
- run an adapter with inline prompt rendering to a local prompt file
- run an adapter with `--prompt-file`
- reject raw `{prompt}` templates by default

## Result

`MH-008` passes for v0.5.

The harness now has provider presets and prompt-file handling without pretending
that Codex or Claude are fully validated on every machine. Presets are installed
as `unknown` until discovery or smoke tests prove them locally.

## Checks

| Check | Command | Result |
| --- | --- | --- |
| Existing DB migration | `.\harness\harness.ps1 --json init` | pass, applied schema version `3` |
| Fresh DB migration | `MY_HARNESS_DB=harness\v05-validation-*.db .\harness\harness.ps1 --json init` | pass, applied schema versions `1,2,3` |
| Preset list | `.\harness\harness.ps1 adapter preset list` | pass |
| Preset install | `.\harness\harness.ps1 --json adapter preset all` | pass |
| Mock discovery | `.\harness\harness.ps1 --json adapter discover --adapter mock-python` | pass, `present`, Python `3.11.15` |
| Inline prompt adapter run | `.\harness\harness.ps1 --json adapter run --adapter mock-python --id MH-008 --prompt ...` | pass |
| Prompt file generated | `Test-Path harness\prompts\MH-008.prompt.txt` | pass |
| Prompt-file adapter run | `.\harness\harness.ps1 --json adapter run --adapter mock-python --id MH-008-FILE --prompt-file ...` | pass |
| Raw prompt guard | `.\harness\harness.ps1 adapter run --adapter raw-prompt-guard ...` | pass, rejected |
| Standard quality gate | `.\harness\harness.ps1 --json check --include-active --strict-active` | pass |

## Recorded Runtime State

- Presets installed: `mock-python`, `codex-local`, `claude-local`
- Discovered adapter: `mock-python`, availability `present`, trust
  `verified_local`
- Story: `MH-008`, status `completed`, evidence `7,8`
- Story: `MH-008-FILE`, status `completed`, evidence `9,10`
- Guard adapter: `raw-prompt-guard`, rejected before runner execution

## Notes

- Prompt files live under `harness/prompts/` and are ignored by git.
- `codex-local` and `claude-local` are presets only; real provider validation is
  deferred.
- Raw `{prompt}` remains available only with `--allow-raw-prompt`.
