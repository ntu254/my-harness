# v0.6 Acceptance Report

Date: 2026-07-19

## Scope

Validate v0.6 shell-free adapter execution and prompt-template behavior:

- migrate existing DB to schema version `4`
- apply schema versions `1`, `2`, `3`, and `4` on a fresh DB
- run `mock-python` in argv mode
- render a prompt template with variables
- record command mode and argv JSON on run rows

## Result

`MH-009` passes for v0.6.

Adapter execution now supports `argv` mode, so provider adapters can avoid shell
parsing when their command can be represented as an argument array.

## Checks

| Check | Command | Result |
| --- | --- | --- |
| Existing DB migration | `.\harness\harness.ps1 --json init` | pass, applied schema version `4` |
| Fresh DB migration | `MY_HARNESS_DB=harness\v06-validation-*.db .\harness\harness.ps1 --json init` | pass, applied schema versions `1,2,3,4` |
| Preset install | `.\harness\harness.ps1 --json adapter preset all` | pass, presets use `argv` mode |
| argv adapter run | `.\harness\harness.ps1 --json adapter run --adapter mock-python --id MH-009 ...` | pass |
| prompt-template adapter run | `.\harness\harness.ps1 --json adapter run --adapter mock-python --id MH-009-TEMPLATE --prompt-template ...` | pass |
| Run metadata | `.\harness\harness.ps1 --json query runs --limit 3` | pass, `command_mode=argv` and argv JSON recorded |
| Standard quality gate | `.\harness\harness.ps1 --json check --include-active --strict-active` | pass |

## Recorded Runtime State

- Story: `MH-009`, status `completed`, evidence `11,12`
- Story: `MH-009-TEMPLATE`, status `completed`, evidence `13,14`
- Run: `6`, command mode `argv`, command argv `["python", "--version"]`
- Run: `7`, command mode `argv`, prompt rendered from
  `templates/prompts/adapter-smoke.md`

## Notes

- Agent commands can now avoid shell parsing in argv mode.
- Verification commands still execute through shell mode.
- Codex and Claude presets still need real local provider smoke before being
  treated as proven.
