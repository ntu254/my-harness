# v0.3 Acceptance Report

Date: 2026-07-19

## Scope

Validate the first local orchestration runner:

- classify one task into a lane
- create intake, story, run, evidence, and trace records
- execute allowed local commands
- run verification command after successful agent command
- block high-risk or approval-required execution unless explicitly allowed
- keep runtime logs out of git

## Result

`MH-006` passes for v0.3.

The runner is intentionally single-task and local-first. It proves the harness
can wrap command execution with process state and evidence before adding daemon,
queue, or provider-specific agent adapters.

## Checks

| Check | Command | Result |
| --- | --- | --- |
| Startup initializes updated schema | `.\scripts\init.ps1` | pass |
| Feature registry parses | `python -m json.tool harness\features.json` | pass |
| CLI syntax compiles | `python -m py_compile cli\harness.py` | pass |
| Runner help available | `.\scripts\harness.ps1 run once --help` | pass |
| Normal runner smoke | `.\scripts\harness.ps1 --json run once --id MH-006 ...` | pass |
| Normal run status | `.\scripts\harness.ps1 --json query runs` | pass, run `1` completed |
| Normal evidence | `.\scripts\harness.ps1 --json query stories` | pass, `MH-006` evidence `3,4` |
| Guarded critical runner smoke | `.\scripts\harness.ps1 --json run once --id MH-006-GUARD ...` | pass |
| Critical run not executed | `Test-Path harness\should-not-exist.txt` | pass, file absent |
| Smoke guard closed | `.\scripts\harness.ps1 story update --id MH-006-GUARD --status cancelled ...` | pass |
| Active queue after smoke | `.\scripts\harness.ps1 --json query active` | pass, `[]` |
| Runtime logs ignored | `git status --short --ignored` | pass, `harness/runs/` ignored |

## Recorded Runtime State

- Normal story: `MH-006`, status `completed`, lane `normal`, revision `4`
- Normal run: `1`, status `completed`, agent exit `0`, verify exit `0`
- Normal evidence: `3,4`
- Guard story: `MH-006-GUARD`, returned `needs_human`, then was closed as
  `cancelled` after the guard behavior was confirmed
- Guard run: `2`, status `needs_human`, command not executed

## Notes

- Runtime logs live under `harness/runs/` and are intentionally ignored.
- `high_risk` and `approval_required` runs require `--allow-high-risk`.
- This version still does not include daemon mode, parallel dispatch, or
  provider-specific Claude/Codex adapters.
