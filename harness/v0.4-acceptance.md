# v0.4 Acceptance Report

Date: 2026-07-19

## Scope

Validate v0.4 adapter, migration, and quality gate behavior:

- migrate existing local DB to schema version `2`
- apply schema versions `1` and `2` on fresh DBs
- run the standard check command
- register a mock adapter
- execute a task through the adapter runner path
- reject inactive adapters before command execution

## Result

`MH-007` passes for v0.4.

The harness now has a stable adapter contract for future Claude and Codex
integration work, while still using the same guarded runner lifecycle.

## Checks

| Check | Command | Result |
| --- | --- | --- |
| Existing DB migration | `.\harness\harness.ps1 --json init` | pass, applied schema version `2` |
| Fresh DB migration | `MY_HARNESS_DB=harness\v04-validation-*.db .\harness\harness.ps1 --json init` | pass, applied schema versions `1,2` |
| CLI syntax compiles | `python -m py_compile cli\harness.py` | pass |
| Standard quality gate | `.\harness\harness.ps1 check --include-active --strict-active` | pass |
| Adapter help available | `.\harness\harness.ps1 adapter run --help` | pass |
| Mock adapter registered | `.\harness\harness.ps1 --json adapter register --id mock-python ...` | pass |
| Mock adapter run | `.\harness\harness.ps1 --json adapter run --adapter mock-python --id MH-007 ...` | pass |
| Adapter metadata recorded | `.\harness\harness.ps1 --json query runs` | pass, `adapter_id` and `prompt` present |
| Inactive adapter rejected | `.\harness\harness.ps1 adapter run --adapter inactive-test ...` | pass, rejected |
| Inactive adapter did not execute | `Test-Path harness\adapter-should-not-exist.txt` | pass, file absent |

## Recorded Runtime State

- Adapter: `mock-python`, provider `mock`, availability `present`
- Story: `MH-007`, status `completed`, lane `normal`
- Run: `3`, status `completed`, adapter `mock-python`
- Evidence: `5,6`
- Guard adapter: `inactive-test`, rejected before execution

## Notes

- Adapter commands are still local shell commands.
- Real Claude/Codex presets are deferred until v0.5 or later.
- Runtime DB and run logs remain ignored local state.
