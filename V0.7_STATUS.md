# v0.7 Status

Status: draft retained, not implemented.

v0.6 is the latest completed and pushed version.

v0.7 should build on v0.6 with:

- provider smoke gates
- adapter capability taxonomy
- shell-free verification commands
- richer provider-specific documentation

The draft migration is kept outside active `state/schema/*.sql` so v0.6 checks
do not accidentally apply v0.7 schema changes.
