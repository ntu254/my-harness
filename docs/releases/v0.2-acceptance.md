# v0.2 Acceptance Report

Date: 2026-07-19

## Scope

Validate the first executable `my-harness` MVP:

- initialize local SQLite state
- record intake, story, evidence, and trace rows
- query active work and stored records
- protect story updates with expected-revision checks
- keep runtime state out of git

## Result

`MH-005` passes for v0.2.

The CLI is intentionally small and local-first. It provides enough durable state
for an AI coding harness to resume work, record evidence, trace decisions, and
avoid stale story updates without introducing service infrastructure yet.

## Checks

| Check | Command | Result |
| --- | --- | --- |
| Startup initializes SQLite | `.\scripts\init.ps1` | pass |
| Feature registry parses | `python -m json.tool harness\features.json` | pass |
| CLI syntax compiles | `python -m py_compile cli\harness.py` | pass |
| CLI initializes DB | `.\scripts\harness.ps1 init` via startup | pass |
| Intake record created | `.\scripts\harness.ps1 intake add ...` | pass, id `1` |
| Story record created | `.\scripts\harness.ps1 story add --id MH-005 ...` | pass |
| Evidence record created | `.\scripts\harness.ps1 evidence add ...` | pass, id `1` |
| Trace record created | `.\scripts\harness.ps1 trace add ...` | pass, id `1` |
| Active work query | `.\scripts\harness.ps1 query active` | pass |
| JSON output query | `.\scripts\harness.ps1 --json query stories` | pass |
| Current revision update | `.\scripts\harness.ps1 story update --id MH-005 --status verifying --expected-revision 1` | pass |
| Stale revision rejection | `.\scripts\harness.ps1 story update --id MH-005 --status completed --expected-revision 1` | pass, rejected |
| Completion update | `.\scripts\harness.ps1 story update --id MH-005 --status completed --expected-revision 2` | pass |

## Recorded Runtime State

- Story: `MH-005`, status `completed`, revision `3`
- Evidence:
  - `1`: startup SQLite smoke check
  - `2`: stale revision rejection check
- Active work query after completion: `(none)`

## Notes

- `harness/harness.db` is local runtime state and is intentionally ignored.
- POSIX wrapper exists at `scripts/harness.sh`, but was not executed in a POSIX
  shell during this Windows session.
- This version is not an orchestrator daemon yet. It is the state spine for the
  next version.
